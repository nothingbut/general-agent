# V3.3 核心 Agent 功能规划

**版本**: V3.3.0
**代号**: Intelligent Core
**基于**: V3.1.0
**目标发布**: 2026-05-15
**开发周期**: 4-6 周

---

## 🎯 战略定位

V3.3 的核心目标是**将 General Agent 从"会话管理工具"升级为"真正的智能代理"**。

**关键转变**:
- ❌ 简单的 LLM 对话工具
- ✅ **具有记忆、学习、规划能力的智能代理**

**核心能力**:
1. **长期记忆** - 记住用户偏好、历史交互、知识积累
2. **上下文压缩** - 智能管理对话历史，突破 token 限制
3. **多模态输入** - 支持文件上传和处理
4. **自动学习** - 从对话中抽取和创建技能
5. **任务规划** - 自主规划和执行复杂任务

---

## 📊 功能价值和优先级分析

### 功能价值矩阵

| 功能 | 用户价值 | 技术复杂度 | 差异化 | 优先级 | 预估工时 |
|------|---------|-----------|--------|--------|---------|
| 长期记忆 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | **P0** | 5-7 天 |
| 上下文压缩 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | **P0** | 4-6 天 |
| 计划任务 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | **P0** | 7-10 天 |
| 上传文件 | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | **P1** | 3-4 天 |
| 抽取 skill | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | **P1** | 5-7 天 |

**优先级说明**:
- **P0**: 核心功能，必须实现，定义产品差异化
- **P1**: 重要功能，增强用户体验

### 推荐实施顺序

**阶段 1** (2 周): 长期记忆 + 上下文压缩
- 原因: 解决最核心的问题（记忆和 token 限制）
- 依赖: 无，可立即开始

**阶段 2** (2 周): 计划任务
- 原因: 最能体现"智能代理"特性
- 依赖: 需要长期记忆支持

**阶段 3** (1-2 周): 上传文件 + 抽取 skill
- 原因: 增强交互和学习能力
- 依赖: 需要上下文压缩支持

---

## 🧠 Phase 1: 长期记忆系统（P0，5-7 天）

### 概述

**目标**: 实现类似 Claude Code 的 auto memory 系统，让 Agent 记住用户的一切。

**核心价值**:
- 记住用户偏好和习惯
- 积累领域知识
- 提供个性化体验
- 跨会话的连续性

### 技术设计

#### 1.1 记忆类型

参考 Claude Code 的分类，定义 5 种记忆类型：

```csharp
public enum MemoryType
{
    User,       // 用户档案（角色、偏好、知识水平）
    Feedback,   // 用户反馈（行为指导）
    Project,    // 项目信息（目标、进展、截止日期）
    Reference,  // 外部资源（文档、链接、工具）
    Knowledge   // 领域知识（概念、最佳实践）
}
```

#### 1.2 数据模型

```csharp
public class Memory
{
    public Guid Id { get; set; }
    public MemoryType Type { get; set; }
    public string Name { get; set; }              // 简短标题
    public string Description { get; set; }       // 一句话描述（用于检索）
    public string Content { get; set; }           // 详细内容（Markdown）
    public DateTime CreatedAt { get; set; }
    public DateTime UpdatedAt { get; set; }
    public Dictionary<string, string> Metadata { get; set; }  // 扩展字段
    public List<string> Tags { get; set; }        // 标签（关联会话）
    public double RelevanceScore { get; set; }    // 相关度评分（用于排序）
}
```

#### 1.3 记忆存储

**存储位置**: `~/.agent/memory/`

**文件组织**:
```
~/.agent/memory/
├── MEMORY.md                  # 索引文件（始终加载）
├── user/
│   ├── profile.md            # 用户档案
│   ├── preferences.md        # 偏好设置
│   └── expertise.md          # 专业领域
├── feedback/
│   ├── coding_style.md       # 代码风格反馈
│   └── interaction.md        # 交互方式反馈
├── project/
│   ├── current_goals.md      # 当前目标
│   └── deadlines.md          # 截止日期
├── reference/
│   └── external_resources.md # 外部资源
└── knowledge/
    ├── domain_concepts.md    # 领域概念
    └── best_practices.md     # 最佳实践
```

#### 1.4 记忆索引（MEMORY.md）

```markdown
# Memory Index

## User
- [Profile](user/profile.md) - 用户角色和背景
- [Preferences](user/preferences.md) - 工作偏好和习惯

## Feedback
- [Coding Style](feedback/coding_style.md) - 代码风格指导
- [Interaction](feedback/interaction.md) - 交互方式偏好

## Project
- [Current Goals](project/current_goals.md) - 当前项目目标
- [Deadlines](project/deadlines.md) - 重要截止日期

## Reference
- [External Resources](reference/external_resources.md) - 文档和工具链接

## Knowledge
- [Domain Concepts](knowledge/domain_concepts.md) - 领域知识积累
- [Best Practices](knowledge/best_practices.md) - 最佳实践总结
```

#### 1.5 自动记忆机制

**触发条件**:
- 用户明确要求（"记住..."）
- 检测到用户偏好表达（"我喜欢..."）
- 检测到重要信息（项目目标、截止日期）
- 用户反馈（"不要...""请继续..."）

**记忆提取流程**:
```
用户消息
   ↓
记忆触发检测（LLM）
   ↓ (触发)
信息提取和分类
   ↓
确定记忆类型
   ↓
写入文件
   ↓
更新 MEMORY.md
   ↓
确认用户
```

#### 1.6 记忆检索

**检索策略**:
1. **始终加载**: `MEMORY.md`（<200 行，总览）
2. **相关性检索**: 基于当前对话主题检索相关记忆
3. **时间衰减**: 最近的记忆权重更高
4. **手动加载**: `/memory load <name>` 命令

**相关性评分**:
```csharp
public double CalculateRelevance(Memory memory, string currentContext)
{
    // 1. 关键词匹配（TF-IDF）
    var keywordScore = TfIdfMatch(memory.Content, currentContext);

    // 2. 标签匹配
    var tagScore = TagMatch(memory.Tags, ExtractTags(currentContext));

    // 3. 时间衰减
    var timeDecay = Math.Exp(-DaysSince(memory.UpdatedAt) / 30.0);

    // 4. 类型权重（User > Feedback > Project > Reference > Knowledge）
    var typeWeight = GetTypeWeight(memory.Type);

    return (keywordScore * 0.4 + tagScore * 0.3) * timeDecay * typeWeight;
}
```

### 实施任务

- [ ] **Task 1.1**: 数据模型和存储
  - Memory 模型定义
  - MemoryRepository 实现
  - 文件系统存储（Markdown + Frontmatter）
  - 预估: 2 天

- [ ] **Task 1.2**: 记忆提取服务
  - MemoryExtractionService（LLM 驱动）
  - 触发条件检测
  - 信息分类和格式化
  - 预估: 2 天

- [ ] **Task 1.3**: 记忆检索服务
  - MemoryRetrievalService
  - 相关性评分算法
  - MEMORY.md 索引管理
  - 预估: 1-2 天

- [ ] **Task 1.4**: CLI 命令
  - `/memory save <内容>` - 手动保存记忆
  - `/memory list` - 列出所有记忆
  - `/memory search <查询>` - 搜索记忆
  - `/memory forget <name>` - 删除记忆
  - 预估: 1 天

### 验收标准

- ✅ 所有记忆类型正确存储和检索
- ✅ 自动记忆准确率 >80%
- ✅ 检索相关性评分合理（人工评估）
- ✅ MEMORY.md 索引始终保持最新
- ✅ 单元测试覆盖率 >90%

---

## 🗜️ Phase 2: 上下文压缩（P0，4-6 天）

### 概述

**目标**: 智能压缩对话历史，突破 token 限制，保持无限对话。

**核心价值**:
- 支持超长对话（1000+ 轮）
- 节省 LLM 成本
- 保留关键信息
- 透明的压缩过程

### 技术设计

#### 2.1 压缩策略

**分层压缩**:
```
完整历史 (0-20 条消息)
   ↓
近期摘要 (20-100 条消息)
   ↓
中期摘要 (100-500 条消息)
   ↓
远期摘要 (500+ 条消息)
```

**压缩触发条件**:
- Token 数量接近限制（80% 阈值）
- 用户手动触发（`/compress`）
- 定期自动压缩（每 50 条消息）

#### 2.2 压缩算法

**层级摘要法**:

```csharp
public class ContextCompressor
{
    // 第一层：保留最近 N 条消息（完整）
    private const int RecentMessageCount = 20;

    // 第二层：中期消息压缩为摘要
    private const int MidTermBatchSize = 10;

    // 第三层：远期消息压缩为单个摘要

    public async Task<CompressedContext> CompressAsync(
        List<Message> messages,
        CancellationToken cancellationToken)
    {
        var compressed = new CompressedContext();

        // 1. 保留最近 20 条（完整）
        compressed.RecentMessages = messages.TakeLast(RecentMessageCount).ToList();

        // 2. 中期消息（20-100 条）每 10 条压缩为摘要
        var midTerm = messages.Skip(Math.Max(0, messages.Count - 100))
                               .Take(Math.Max(0, messages.Count - RecentMessageCount))
                               .ToList();
        compressed.MidTermSummaries = await CompressBatches(midTerm, MidTermBatchSize);

        // 3. 远期消息（100+ 条）压缩为单个摘要
        var longTerm = messages.Take(Math.Max(0, messages.Count - 100)).ToList();
        if (longTerm.Any())
        {
            compressed.LongTermSummary = await CompressToSingleSummary(longTerm);
        }

        return compressed;
    }

    private async Task<List<string>> CompressBatches(
        List<Message> messages,
        int batchSize)
    {
        var summaries = new List<string>();
        for (int i = 0; i < messages.Count; i += batchSize)
        {
            var batch = messages.Skip(i).Take(batchSize).ToList();
            var summary = await SummarizeBatch(batch);
            summaries.Add(summary);
        }
        return summaries;
    }

    private async Task<string> CompressToSingleSummary(List<Message> messages)
    {
        // 使用 LLM 生成摘要
        var prompt = $@"
请总结以下对话的关键信息：

{FormatMessages(messages)}

要求：
1. 提取重要决策和结论
2. 保留关键上下文
3. 记录未完成的任务
4. 控制在 500 字以内";

        return await _llm.GenerateAsync(prompt);
    }
}
```

#### 2.3 数据模型

```csharp
public class CompressedContext
{
    public string SessionId { get; set; }
    public List<Message> RecentMessages { get; set; }          // 最近 20 条（完整）
    public List<string> MidTermSummaries { get; set; }         // 中期摘要
    public string LongTermSummary { get; set; }                // 远期摘要
    public int OriginalMessageCount { get; set; }              // 原始消息数
    public int CompressedTokenCount { get; set; }              // 压缩后 token 数
    public DateTime CompressedAt { get; set; }
    public CompressionMetadata Metadata { get; set; }
}

public class CompressionMetadata
{
    public int OriginalTokens { get; set; }
    public int CompressedTokens { get; set; }
    public double CompressionRatio { get; set; }               // 压缩比
    public List<string> LostInformation { get; set; }          // 丢失的信息（供用户审查）
    public List<string> KeyDecisions { get; set; }             // 关键决策
    public List<string> PendingTasks { get; set; }             // 未完成任务
}
```

#### 2.4 压缩质量保证

**关键信息提取**:
- 用户明确的要求和指示
- 重要的决策和结论
- 未完成的任务
- 关键的上下文信息

**丢失检测**:
```csharp
public async Task<List<string>> DetectLostInformation(
    List<Message> original,
    CompressedContext compressed)
{
    var prompt = $@"
原始对话:
{FormatMessages(original)}

压缩后的摘要:
{FormatCompressed(compressed)}

请列出压缩过程中丢失的重要信息（如果有）：";

    var response = await _llm.GenerateAsync(prompt);
    return ParseLostInformation(response);
}
```

### 实施任务

- [ ] **Task 2.1**: 压缩算法实现
  - ContextCompressor 类
  - 分层压缩逻辑
  - Token 计数和阈值检测
  - 预估: 2 天

- [ ] **Task 2.2**: LLM 摘要生成
  - 批量摘要生成
  - 单摘要生成
  - 摘要质量评估
  - 预估: 2 天

- [ ] **Task 2.3**: 压缩元数据管理
  - 压缩历史记录
  - 丢失信息检测
  - 压缩可视化（显示压缩比）
  - 预估: 1 天

- [ ] **Task 2.4**: CLI 命令
  - `/compress` - 手动压缩
  - `/compress --auto` - 启用自动压缩
  - `/compress --show` - 显示压缩统计
  - `/uncompress` - 恢复原始消息（如果可能）
  - 预估: 1 天

### 验收标准

- ✅ 压缩比达到 5:1（平均）
- ✅ 关键信息保留率 >95%
- ✅ 压缩后对话连贯性良好（人工评估）
- ✅ 支持 1000+ 轮对话
- ✅ 单元测试覆盖率 >90%

---

## 📋 Phase 3: 计划任务系统（P0，7-10 天）

### 概述

**目标**: 实现自主规划和执行复杂任务的能力，类似 AutoGPT/BabyAGI。

**核心价值**:
- 分解复杂任务
- 自主执行
- 进度跟踪
- 错误恢复

### 技术设计

#### 3.1 任务模型

```csharp
public class Task
{
    public Guid Id { get; set; }
    public string Title { get; set; }
    public string Description { get; set; }
    public TaskType Type { get; set; }                     // Research, Code, Test, Document
    public TaskStatus Status { get; set; }                 // Pending, InProgress, Completed, Failed
    public int Priority { get; set; }                      // 1-5
    public List<Guid> Dependencies { get; set; }           // 依赖任务 ID
    public List<string> Tags { get; set; }
    public DateTime CreatedAt { get; set; }
    public DateTime? StartedAt { get; set; }
    public DateTime? CompletedAt { get; set; }
    public TaskResult Result { get; set; }
    public List<TaskStep> Steps { get; set; }              // 子步骤
}

public class TaskStep
{
    public Guid Id { get; set; }
    public string Description { get; set; }
    public StepType Type { get; set; }                     // Command, LLM, UserInput
    public string Command { get; set; }                    // 执行的命令或提示词
    public string Output { get; set; }                     // 执行结果
    public StepStatus Status { get; set; }
    public DateTime? ExecutedAt { get; set; }
}

public class TaskResult
{
    public bool Success { get; set; }
    public string Summary { get; set; }                    // 结果摘要
    public List<string> Artifacts { get; set; }            // 产物（文件路径）
    public string ErrorMessage { get; set; }
    public int RetryCount { get; set; }
}
```

#### 3.2 任务规划器

```csharp
public class TaskPlanner
{
    private readonly ILLMClient _llm;

    public async Task<Plan> CreatePlanAsync(string goal)
    {
        var prompt = $@"
用户目标: {goal}

请创建详细的任务计划：

1. 将目标分解为具体的任务（Task）
2. 为每个任务定义：
   - 标题和描述
   - 任务类型（Research/Code/Test/Document）
   - 优先级（1-5）
   - 依赖关系
3. 按依赖关系排序

输出格式（JSON）：
{{
  ""tasks"": [
    {{
      ""title"": ""任务标题"",
      ""description"": ""详细描述"",
      ""type"": ""Code"",
      ""priority"": 3,
      ""dependencies"": []
    }}
  ]
}}";

        var response = await _llm.GenerateAsync(prompt);
        return ParsePlan(response);
    }

    public async Task<List<TaskStep>> GenerateStepsAsync(Task task)
    {
        var prompt = $@"
任务: {task.Title}
描述: {task.Description}

请生成执行该任务的具体步骤：

1. 每个步骤描述要具体可执行
2. 步骤类型：Command（命令行）、LLM（LLM 生成）、UserInput（需要用户输入）
3. 对于 Command 步骤，提供具体命令

输出格式（JSON）：
{{
  ""steps"": [
    {{
      ""description"": ""步骤描述"",
      ""type"": ""Command"",
      ""command"": ""dotnet build""
    }}
  ]
}}";

        var response = await _llm.GenerateAsync(prompt);
        return ParseSteps(response);
    }
}
```

#### 3.3 任务执行器

```csharp
public class TaskExecutor
{
    private readonly ILLMClient _llm;
    private readonly IShellExecutor _shell;
    private readonly ITaskRepository _taskRepo;

    public async Task<TaskResult> ExecuteTaskAsync(
        Task task,
        CancellationToken cancellationToken)
    {
        try
        {
            // 1. 更新状态
            task.Status = TaskStatus.InProgress;
            task.StartedAt = DateTime.UtcNow;
            await _taskRepo.UpdateAsync(task);

            // 2. 执行每个步骤
            foreach (var step in task.Steps)
            {
                var stepResult = await ExecuteStepAsync(step, cancellationToken);
                step.Output = stepResult.Output;
                step.Status = stepResult.Success ? StepStatus.Completed : StepStatus.Failed;
                step.ExecutedAt = DateTime.UtcNow;

                if (!stepResult.Success)
                {
                    // 失败处理：重试或跳过
                    var shouldRetry = await DecideRetryAsync(task, step);
                    if (shouldRetry && task.Result.RetryCount < 3)
                    {
                        task.Result.RetryCount++;
                        return await ExecuteTaskAsync(task, cancellationToken);
                    }
                    else
                    {
                        throw new TaskExecutionException($"Step failed: {step.Description}");
                    }
                }

                await _taskRepo.UpdateAsync(task);
            }

            // 3. 完成任务
            task.Status = TaskStatus.Completed;
            task.CompletedAt = DateTime.UtcNow;
            task.Result.Success = true;
            task.Result.Summary = await GenerateSummaryAsync(task);

            await _taskRepo.UpdateAsync(task);
            return task.Result;
        }
        catch (Exception ex)
        {
            task.Status = TaskStatus.Failed;
            task.Result.Success = false;
            task.Result.ErrorMessage = ex.Message;
            await _taskRepo.UpdateAsync(task);
            throw;
        }
    }

    private async Task<StepResult> ExecuteStepAsync(
        TaskStep step,
        CancellationToken cancellationToken)
    {
        return step.Type switch
        {
            StepType.Command => await ExecuteCommandStepAsync(step, cancellationToken),
            StepType.LLM => await ExecuteLLMStepAsync(step, cancellationToken),
            StepType.UserInput => await ExecuteUserInputStepAsync(step, cancellationToken),
            _ => throw new NotSupportedException($"Step type {step.Type} not supported")
        };
    }

    private async Task<StepResult> ExecuteCommandStepAsync(
        TaskStep step,
        CancellationToken cancellationToken)
    {
        var result = await _shell.ExecuteAsync(step.Command, cancellationToken);
        return new StepResult
        {
            Success = result.ExitCode == 0,
            Output = result.Output
        };
    }

    private async Task<StepResult> ExecuteLLMStepAsync(
        TaskStep step,
        CancellationToken cancellationToken)
    {
        var response = await _llm.GenerateAsync(step.Command, cancellationToken);
        return new StepResult
        {
            Success = true,
            Output = response
        };
    }

    private async Task<StepResult> ExecuteUserInputStepAsync(
        TaskStep step,
        CancellationToken cancellationToken)
    {
        // 提示用户输入
        Console.WriteLine($"[User Input Required] {step.Description}");
        Console.Write("> ");
        var input = Console.ReadLine();

        return new StepResult
        {
            Success = !string.IsNullOrEmpty(input),
            Output = input
        };
    }
}
```

#### 3.4 依赖管理

```csharp
public class DependencyResolver
{
    public List<Task> ResolveDependencies(List<Task> tasks)
    {
        // 拓扑排序
        var sorted = new List<Task>();
        var visited = new HashSet<Guid>();
        var visiting = new HashSet<Guid>();

        foreach (var task in tasks)
        {
            Visit(task, tasks, sorted, visited, visiting);
        }

        return sorted;
    }

    private void Visit(
        Task task,
        List<Task> allTasks,
        List<Task> sorted,
        HashSet<Guid> visited,
        HashSet<Guid> visiting)
    {
        if (visited.Contains(task.Id)) return;
        if (visiting.Contains(task.Id))
            throw new InvalidOperationException("Circular dependency detected");

        visiting.Add(task.Id);

        foreach (var depId in task.Dependencies)
        {
            var depTask = allTasks.First(t => t.Id == depId);
            Visit(depTask, allTasks, sorted, visited, visiting);
        }

        visiting.Remove(task.Id);
        visited.Add(task.Id);
        sorted.Add(task);
    }
}
```

### 实施任务

- [ ] **Task 3.1**: 任务模型和存储
  - Task 和 TaskStep 模型
  - TaskRepository 实现
  - SQLite 持久化
  - 预估: 2 天

- [ ] **Task 3.2**: 任务规划器
  - TaskPlanner 实现
  - LLM 驱动的任务分解
  - 依赖关系分析
  - 预估: 2-3 天

- [ ] **Task 3.3**: 任务执行器
  - TaskExecutor 实现
  - 步骤执行（Command/LLM/UserInput）
  - 错误处理和重试
  - 预估: 2-3 天

- [ ] **Task 3.4**: CLI 命令
  - `/plan <目标>` - 创建计划
  - `/plan show` - 显示当前计划
  - `/task start <id>` - 开始执行任务
  - `/task stop <id>` - 停止任务
  - `/task list` - 列出所有任务
  - 预估: 1-2 天

### 验收标准

- ✅ 任务规划准确率 >80%
- ✅ 任务执行成功率 >90%（简单任务）
- ✅ 依赖关系正确解析
- ✅ 支持中断和恢复
- ✅ 单元测试覆盖率 >90%

---

## 📁 Phase 4: 上传文件（P1，3-4 天）

### 概述

**目标**: 支持文件上传和处理，增强多模态交互能力。

**核心价值**:
- 图片识别和分析
- PDF 文档解析
- 代码文件分析
- 数据文件处理

### 技术设计

#### 4.1 支持的文件类型

| 类型 | 格式 | 处理方式 |
|------|------|---------|
| 图片 | PNG, JPG, GIF | Claude Vision API |
| 文档 | PDF, DOCX, TXT | 文本提取 |
| 代码 | All text files | 直接读取 |
| 数据 | CSV, JSON, XML | 结构化解析 |

#### 4.2 数据模型

```csharp
public class FileAttachment
{
    public Guid Id { get; set; }
    public string SessionId { get; set; }
    public string FileName { get; set; }
    public string FilePath { get; set; }                   // 存储路径
    public string FileType { get; set; }                   // MIME type
    public long FileSize { get; set; }
    public DateTime UploadedAt { get; set; }
    public FileProcessingResult ProcessingResult { get; set; }
}

public class FileProcessingResult
{
    public bool Success { get; set; }
    public string ExtractedText { get; set; }              // 提取的文本
    public string Summary { get; set; }                    // LLM 生成的摘要
    public Dictionary<string, object> Metadata { get; set; } // 元数据
    public string ErrorMessage { get; set; }
}
```

#### 4.3 文件处理器

```csharp
public interface IFileProcessor
{
    Task<FileProcessingResult> ProcessAsync(FileAttachment file);
}

public class ImageProcessor : IFileProcessor
{
    private readonly ILLMClient _llm;

    public async Task<FileProcessingResult> ProcessAsync(FileAttachment file)
    {
        // 使用 Claude Vision API
        var imageData = await File.ReadAllBytesAsync(file.FilePath);
        var base64 = Convert.ToBase64String(imageData);

        var response = await _llm.AnalyzeImageAsync(base64,
            "请描述这张图片的内容，并提取关键信息。");

        return new FileProcessingResult
        {
            Success = true,
            ExtractedText = response,
            Summary = response
        };
    }
}

public class PdfProcessor : IFileProcessor
{
    public async Task<FileProcessingResult> ProcessAsync(FileAttachment file)
    {
        // 使用 PDF 解析库（如 iTextSharp 或 PdfPig）
        var text = await ExtractTextFromPdfAsync(file.FilePath);

        // 如果文本过长，使用 LLM 生成摘要
        var summary = text.Length > 5000
            ? await _llm.SummarizeAsync(text)
            : text;

        return new FileProcessingResult
        {
            Success = true,
            ExtractedText = text,
            Summary = summary
        };
    }
}
```

### 实施任务

- [ ] **Task 4.1**: 文件上传管理
  - FileAttachment 模型
  - 文件存储（~/.agent/files/）
  - FileRepository 实现
  - 预估: 1 天

- [ ] **Task 4.2**: 文件处理器
  - ImageProcessor（Claude Vision）
  - PdfProcessor（文本提取）
  - CodeProcessor（语法高亮）
  - DataProcessor（CSV/JSON 解析）
  - 预估: 2 天

- [ ] **Task 4.3**: CLI 命令
  - `/upload <文件路径>` - 上传文件
  - `/file list` - 列出已上传文件
  - `/file show <id>` - 查看文件内容
  - `/file delete <id>` - 删除文件
  - 预估: 1 天

### 验收标准

- ✅ 支持所有主要文件类型
- ✅ 图片识别准确率 >90%
- ✅ PDF 文本提取准确率 >95%
- ✅ 文件上传速度 <3s（10MB 以内）
- ✅ 单元测试覆盖率 >90%

---

## 🎓 Phase 5: 对话中抽取 Skill（P1，5-7 天）

### 概述

**目标**: 从对话中自动学习和创建技能，实现自我进化。

**核心价值**:
- 自动化重复任务
- 知识积累
- 技能库扩展
- 个性化定制

### 技术设计

#### 5.1 技能抽取流程

```
对话监控
   ↓
模式识别（检测重复任务）
   ↓
技能候选提取
   ↓
用户确认
   ↓
技能模板生成
   ↓
保存为 .md 文件
   ↓
技能注册
```

#### 5.2 模式识别

**触发条件**:
- 相似的对话模式（>2 次）
- 用户明确表示重复性（"像上次一样..."）
- 固定的工作流程
- 复杂的多步骤操作

**示例**:
```
用户: "帮我生成一个 C# 类，包含 ID、Name、CreatedAt 属性"
助手: "好的..."（生成代码）

（第二次）
用户: "再生成一个 Product 类，也是 ID、Name、CreatedAt"
助手: "检测到重复模式，是否创建技能？"
```

#### 5.3 技能模板生成

```csharp
public class SkillExtractor
{
    private readonly ILLMClient _llm;

    public async Task<SkillTemplate> ExtractSkillAsync(
        List<Message> conversationHistory)
    {
        var prompt = $@"
以下是用户的对话历史：

{FormatMessages(conversationHistory)}

请分析是否存在可以抽取为技能的模式：

1. 识别重复的任务
2. 提取任务的参数
3. 生成技能定义

输出格式（YAML + Markdown）：
---
name: skill_name
description: 技能描述
parameters:
  - name: param1
    type: string
    required: true
    description: 参数描述
---

{{param1}} 的提示词模板
";

        var response = await _llm.GenerateAsync(prompt);
        return ParseSkillTemplate(response);
    }
}
```

#### 5.4 技能模板

```markdown
---
name: generate_entity_class
description: 生成实体类（C#）
parameters:
  - name: class_name
    type: string
    required: true
    description: 类名
  - name: properties
    type: array
    required: false
    description: 额外属性（默认包含 Id, Name, CreatedAt）
---

请生成一个 C# 实体类：

类名: {class_name}

基础属性:
- public Guid Id { get; set; }
- public string Name { get; set; }
- public DateTime CreatedAt { get; set; }

{{#if properties}}
额外属性:
{{#each properties}}
- {this}
{{/each}}
{{/if}}

要求:
- 使用 C# 10 语法
- 包含必要的 using 语句
- 添加简单注释
```

### 实施任务

- [ ] **Task 5.1**: 对话模式识别
  - PatternDetector 实现
  - 相似度计算
  - 触发条件判断
  - 预估: 2 天

- [ ] **Task 5.2**: 技能提取器
  - SkillExtractor 实现
  - LLM 驱动的技能生成
  - 参数提取
  - 预估: 2-3 天

- [ ] **Task 5.3**: 技能管理
  - 技能保存（.md 文件）
  - 技能注册
  - 技能测试
  - 预估: 1-2 天

- [ ] **Task 5.4**: CLI 命令
  - `/skill extract` - 手动抽取技能
  - `/skill test <name>` - 测试技能
  - `/skill edit <name>` - 编辑技能
  - 预估: 1 天

### 验收标准

- ✅ 模式识别准确率 >70%
- ✅ 技能生成正确率 >80%
- ✅ 生成的技能可直接使用
- ✅ 用户确认机制完善
- ✅ 单元测试覆盖率 >90%

---

## 📊 V3.3 总体规划

### 实施时间线

```
Week 1-2: Phase 1 (长期记忆) + Phase 2 (上下文压缩)
Week 3-4: Phase 3 (计划任务)
Week 5: Phase 4 (上传文件) + Phase 5 (抽取 Skill)
Week 6: 集成测试、优化、文档
```

### 里程碑

**Milestone 1: 智能记忆 (Week 2)**
- ✅ 长期记忆系统完成
- ✅ 上下文压缩完成
- 交付物: 具有记忆和无限对话能力的 Agent

**Milestone 2: 自主规划 (Week 4)**
- ✅ 计划任务系统完成
- 交付物: 能够自主执行复杂任务的 Agent

**Milestone 3: 完整功能 (Week 5)**
- ✅ 文件上传完成
- ✅ 技能抽取完成
- 交付物: 完整的智能代理系统

**Milestone 4: 发布就绪 (Week 6)**
- ✅ 集成测试完成
- ✅ 性能优化完成
- ✅ 文档完善
- 交付物: V3.3.0 正式发布

### 优先级调整建议

**如果时间紧张，优先级排序**:
1. **P0-High**: 长期记忆（最基础）
2. **P0-High**: 上下文压缩（解决痛点）
3. **P0-High**: 计划任务（核心差异化）
4. **P1-Medium**: 上传文件（增强交互）
5. **P1-Low**: 抽取 Skill（锦上添花）

**最小可行产品（MVP）**: Phase 1 + Phase 2
**完整版本**: 全部 5 个 Phase

---

## 🎯 与 V3.2 的关系

**建议**: 暂缓 V3.2 搜索功能完善，优先实施 V3.3 核心 Agent 功能。

**理由**:
1. **战略价值**: V3.3 功能定义产品差异化
2. **用户痛点**: 记忆和规划比搜索更核心
3. **技术基础**: V3.1 已有搜索框架，可后续完善

**折中方案**:
- 快速修复 V3.2 Phase 1.1（FTS5 搜索逻辑，2-3 天）
- 然后全力投入 V3.3

---

## 📝 下一步行动

### 立即可做

1. **确认优先级**:
   - 用户反馈这 5 个功能的优先级排序
   - 确定是否暂缓 V3.2

2. **创建实施计划**:
   ```bash
   # 创建 V3.3 feature 分支
   git checkout -b feature/v3.3-core-agent

   # 创建详细实施计划
   cat > docs/superpowers/plans/2026-04-01-v3.3-core-agent.md
   ```

3. **技术调研**:
   - 长期记忆存储方案（文件 vs 数据库）
   - 上下文压缩算法（总结 vs 删除）
   - 任务执行安全性（沙箱）

### 推荐开始顺序

**Week 1: Phase 1 长期记忆**
- 最基础的功能
- 后续功能的依赖
- 技术复杂度适中

---

**创建日期**: 2026-03-26
**基于版本**: V3.1.0
**目标版本**: V3.3.0
**维护者**: General Agent Team
